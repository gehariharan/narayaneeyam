export const escapeHtml = value => String(value ?? '').replace(/[&<>"']/g, c => ({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
const css = `:root{color-scheme:light;--ink:#392719;--paper:#f4ebdb;--line:#d5bd98;--accent:#793e2d}*{box-sizing:border-box}body{font:16px/1.5 system-ui,sans-serif;background:var(--paper);color:var(--ink);margin:0}header,main{max-width:1800px;margin:auto;padding:.7rem 2vw}header{border-bottom:1px solid var(--line);display:flex;gap:.8rem;justify-content:space-between;align-items:center;flex-wrap:wrap}a{color:var(--accent);text-underline-offset:3px}nav{display:flex;gap:.8rem;flex-wrap:wrap}h1,h2{font-family:Georgia,serif;line-height:1.2}h1{font-size:2rem;margin:.35rem 0 .6rem}h2{font-size:1.1rem;margin:0 0 .45rem}p{margin:.5rem 0}button,textarea{font:inherit}button{border:0;border-radius:4px;padding:.45rem .85rem;background:var(--accent);color:white;cursor:pointer}button:disabled{opacity:.6}.cards{display:grid;grid-template-columns:repeat(auto-fit,minmax(280px,1fr));gap:1rem;max-width:1100px}.card{display:block;text-decoration:none;border:1px solid var(--line);background:#fff8eb;border-radius:8px;overflow:hidden}.card img{width:100%;aspect-ratio:16/9;object-fit:contain;background:#e7d5b8}.card div{padding:.7rem 1rem}.card h2{font-size:1.4rem}.card p{font-size:.9rem}.sloka{border-top:1px solid var(--line);padding:.8rem 0;scroll-margin-top:.5rem}.triple{display:grid;grid-template-columns:repeat(3,minmax(0,1fr));gap:.65rem}figure{margin:0;min-width:0}figcaption{font-size:.8rem;color:#695541;margin-bottom:.25rem}.art{display:block;width:100%;aspect-ratio:16/9;object-fit:contain;background:#e5d5bc;border:1px solid var(--line);cursor:zoom-in}.missing{display:grid;place-items:center;text-align:center;aspect-ratio:16/9;border:1px dashed #ad9574;color:#77644f;font-size:.85rem}.commentary{margin:.45rem 0 0;padding:.5rem .7rem;background:#fff8eb;border-left:2px solid #b88446;white-space:pre-line}.feedback{font-size:.8rem;margin:.2rem 0}.feedback summary{cursor:pointer;color:var(--accent);width:fit-content;margin-left:auto;list-style:none;text-decoration:underline}.feedback summary::-webkit-details-marker{display:none}.feedback form{max-width:680px;margin:.35rem 0 .5rem auto}.feedback textarea{display:block;width:100%;min-height:75px;padding:.55rem;background:#fffcf6;border:1px solid #a98d6b;border-radius:4px;resize:vertical;margin-bottom:.35rem}.feedback .status{margin-left:.5rem}.jump{gap:.4rem;margin-bottom:.6rem}.jump a{font-size:.8rem;padding:.15rem .45rem;border:1px solid var(--line);border-radius:3px;text-decoration:none}.trap{position:absolute;left:-10000px}.pager{padding:.6rem 0;font-size:.85rem}dialog{border:0;background:#221b14;color:white;padding:.7rem;max-width:96vw;max-height:96vh}dialog::backdrop{background:#000c}dialog img{max-width:91vw;max-height:84vh;object-fit:contain}dialog button{display:block;margin:0 0 .4rem auto}:focus-visible{outline:3px solid #2e7890;outline-offset:3px}@media(max-width:650px){header,main{padding:.6rem .7rem}.triple{grid-template-columns:minmax(0,1fr);gap:.8rem}h1{font-size:1.65rem}.commentary{font-size:.9rem}.feedback textarea{font-size:16px}.feedback summary{padding:.6rem 0}.jump a{min-width:36px;min-height:36px;display:grid;place-items:center}.cards{grid-template-columns:minmax(0,1fr)}}`;
const client = `const modal=document.querySelector('dialog');document.querySelectorAll('a[data-zoom]').forEach(a=>a.addEventListener('click',e=>{e.preventDefault();modal.querySelector('img').src=a.href;modal.querySelector('img').alt=a.querySelector('img').alt;modal.showModal()}));modal?.querySelector('button').addEventListener('click',()=>modal.close());modal?.addEventListener('click',e=>{if(e.target===modal)modal.close()});document.querySelectorAll('form[data-feedback]').forEach(form=>form.addEventListener('submit',async e=>{e.preventDefault();const status=form.querySelector('.status'),button=form.querySelector('button');button.disabled=true;status.textContent='Saving…';try{const body=Object.fromEntries(new FormData(form));body.daskam=Number(body.daskam);body.sloka=Number(body.sloka);const response=await fetch(form.action,{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(body)});const result=await response.json();if(!response.ok)throw new Error(result.error||'Could not save. Please retry.');status.textContent='Saved. Thank you.';form.querySelector('textarea').value='';}catch(error){status.textContent=error.message}finally{button.disabled=false}}));`;
function shell(title, body, base, live) {return `<!doctype html><html lang="en"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><meta name="robots" content="noindex,nofollow"><title>${escapeHtml(title)} · Narayaneeyam</title><style>${css}</style></head><body><header><a href="${base}${live?'':'index.html'}">Narayaneeyam</a><nav><a href="${base}d001${live?'':'.html'}">Dasakam 1</a><a href="${base}d002${live?'':'.html'}">Dasakam 2</a></nav></header><main>${body}</main><dialog aria-label="Enlarged artwork"><button type="button">Close</button><img alt=""></dialog><script>${client}</script></body></html>`;}
export function renderIndex(data,base='/narayaneeyam/',live=true){return shell('Narayaneeyam',`<h1>Narayaneeyam</h1><div class="cards">${data.chapters.map(c=>`<a class="card" href="${base}d00${c.id}${live?'':'.html'}"><img src="${base}${c.representative}" alt="${escapeHtml(c.title)}"><div><h2>${escapeHtml(c.title)}</h2><p>${escapeHtml(c.description)}</p></div></a>`).join('')}</div>`,base,live);}
const imageLabel = img => ({reference:'Reference',matte:'Traditional mural',glossy:'Detailed painting'}[img.role] || img.label);
export function renderChapter(chapter,revision,base='/narayaneeyam/',live=true){
 const rows=chapter.rows.map(row=>`<section class="sloka" id="s${String(row.n).padStart(3,'0')}"><h2>Sloka ${String(row.n).padStart(3,'0')}${row.title?' · '+escapeHtml(row.title):''}</h2><div class="triple">${row.images.map(img=>`<figure><figcaption>${escapeHtml(imageLabel(img))}</figcaption>${img.key?`<a data-zoom href="${base}${img.key}"><img class="art" src="${base}${img.key}" loading="${row.n===1?'eager':'lazy'}" decoding="async" alt="${escapeHtml(chapter.title)} sloka ${row.n}: ${escapeHtml(imageLabel(img))}"></a>`:'<div class="missing">Coming soon</div>'}</figure>`).join('')}</div>${live?`<details class="feedback"><summary>Leave feedback</summary><form data-feedback action="${base}api/feedback"><input type="hidden" name="daskam" value="${chapter.id}"><input type="hidden" name="sloka" value="${row.n}"><input type="hidden" name="revision" value="${revision}"><label class="trap" aria-hidden="true">Website<input name="website" tabindex="-1" autocomplete="off"></label><textarea name="message" aria-label="Feedback on generated artwork for sloka ${row.n}" required minlength="3" maxlength="2000" placeholder="Feedback on the generated artwork…"></textarea><button type="submit">Submit</button><output class="status" aria-live="polite"></output></form></details>`:''}<p class="commentary">${escapeHtml(row.commentary||'')}</p></section>`).join('');
 return shell(chapter.title,`<h1>${escapeHtml(chapter.title)}</h1><nav class="jump" aria-label="Jump to sloka">${chapter.rows.map(r=>`<a href="#s${String(r.n).padStart(3,'0')}">${String(r.n).padStart(3,'0')}</a>`).join('')}</nav>${rows}<nav class="pager"><a href="${base}${live?'':'index.html'}">← All Dasakams</a><a href="${base}d00${chapter.id===1?2:1}${live?'':'.html'}">Dasakam ${chapter.id===1?2:1} →</a></nav>`,base,live);
}
